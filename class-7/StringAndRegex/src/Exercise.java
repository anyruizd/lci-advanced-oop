public class Exercise {
    public static void main(String[] args) {
        // * next word is gonna be ??? we can combine with other characters
        // if i put s it means the next word should be s

        // 1.
        // 2. "^[^\s]+$"
        // [^\s\d] no white spaces
    }
}
